---
description: Resume cost-guard halting after a pause. Restores the master enabled switch to true.
disable-model-invocation: true
---

# cost-guard: resume halts

The CLI output is below:

!`CLAUDE_PLUGIN_DATA="${CLAUDE_PLUGIN_DATA}" CLAUDE_PLUGIN_ROOT="${CLAUDE_PLUGIN_ROOT}" "${CLAUDE_PLUGIN_ROOT}/bin/cost-guard" resume`

Confirm to the user in a single short sentence that cost-guard is enabled again. Do not add any other commentary.
